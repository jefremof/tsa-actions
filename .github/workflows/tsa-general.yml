on:
    workflow_call:
      inputs:
        args:
          required: true
          type: string

jobs:
  run-tsa-cli:
    runs-on: ubuntu-22.04
    permissions:
      security-events: write
      actions: read
      contents: read
    container:
      image: ghcr.io/jefremof/tsa/tsa-physical-locations:latest

    steps:
        - name: 'Checkout repository'
          uses: actions/checkout@v4
          with:
            submodules: recursive

        - name: Run TON symbolic analyzer CLI
          run: java -jar /home/tsa.jar ${{ inputs.args }} > output-${{ github.run_id }}-${{ github.run_number }}-${{ github.run_attempt }}.sarif

        - name: Upload SARIF report
          uses: GitHub/codeql-action/upload-sarif@v3
          with:
            sarif_file: output-${{ github.run_id }}-${{ github.run_number }}-${{ github.run_attempt }}.sarif